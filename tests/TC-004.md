# TC-004 — Project Count Limit (Level B: Source Fidelity)

## Metadata
- **Level**: B (source fidelity) — **PRE-LAUNCH GATE**
- **Capability**: ST.CAP.04
- **Executor model**: Haiku 4.5
- **Context layers**: L1 + L2 (method/04) ONLY — **no sources** (ST.DRR-005)
- **Tester role**: MetaAgent#EvidenceAuditor:meta-agent
- **Source rule under test**: method/04 — "Keep active project count manageable (2-5 recommended)"

## Purpose
Adversarial test: verifies that the agent follows the active project count rule even without raw sources in context. The user describes 8 projects (exceeding the 2-5 norm).

If the agent accepts all 8 without a warning → MethodDescription does not contain a sufficient rule statement → fix method/04 and re-run.

## Input
- **User message**: contents of `fixtures/overloaded-projects.md`
- **System prompt**: CLAUDE.md + bounded-context + method/04
- **Important**: sources/08-strategizing are NOT loaded

## Adversarial scenario
The user describes 8 active projects (P1-P8) and asks for prioritization.
The agent must:
1. Notice that the number of active projects exceeds the recommended count (2-5)
2. Warn the user about overload
3. Propose a reduction strategy: grouping, archiving, status change
4. End with <= 5 priority projects

## Acceptance criteria

| # | Criterion | PASS | FAIL |
|---|-----------|------|------|
| 1 | Agent notices: project count exceeds recommended | Explicit mention of excess | Silently accepts all 8 |
| 2 | Agent warns about overload | Warning/comment present | No mention of the problem |
| 3 | Reduction strategy proposed | Grouping / archive / pause | Only a priority list of 8 |
| 4 | Final priority list <= 5 | <= 5 highlighted as priority | > 5 equally "priority" |
| 5 | Output <= 4096 characters | — | — |
| 6 | No markdown tables | — | — |

**Gate condition**: all criteria 1-4 must be PASS.

## Execution instructions
1. Assemble system prompt: CLAUDE.md + bounded-context + method/04 (L1+L2 only, no sources)
2. Submit `fixtures/overloaded-projects.md` as user message
3. Get response from Haiku 4.5
4. Check each criterion (pass/fail)
5. If FAIL on criteria 1-3: method/04 does not contain a sufficient rule statement
   → add explicit rule to method/04 → re-run TC-004
6. Record result: date, model, call cost, pass/fail per criterion

## Pre-launch gate status
- [ ] Test run 1: ___ (date, model, method/04 version)
- [ ] Gate PASSED → production launch approved

## Source fidelity notes
Source rule origin: sources/self-development-methods/08-strategizing/ (section on projects list).
Rule carrier: method/04 ("Keep active project count manageable (2-5 recommended)").
If test fails: strengthen rule in method/04. Do NOT revert to loading raw sources as fix.
